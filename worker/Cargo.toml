[package]
name = "worker"
version = "0.1.0"
edition = "2024"

[dependencies]
dfir_rs = { git = "https://github.com/hydro-project/hydro.git", branch = "main", features = ["deploy_integration"]  }
tokio = { version = "1", features = ["full"] }
